#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{
    // int n = 5;
    // int *p = &n;
    // (*p)++;
    // cout << *p << endl;

    // double d = 4.345;
    // double *ptr = &d;
    // cout << *ptr << endl;
    // cout << "size of num " << sizeof(n) << endl;
    // cout << "size of num " << sizeof(ptr) << endl;
    // int *pt = 0;

    // cout << *pt;
    // int i = 5;
    // int *p = 0;
    // p = &i;

    // cout << p << endl;

    // int num = 5;
    // int a = num;
    // a++;
    // cout << num << endl;

    int arr[10];
    cout << "address of first memory block is " << &arr << endl;
    cout << "address of first memory block is " << &arr[1] << endl;

    return 0;
}
